import React from 'react';
import { List } from '../../components';

/**
 * ListScreen Component
 *
 * This component serves as a wrapper for the `List` component, which displays a list
 * of items. It acts as a dedicated screen in the navigation flow of the application.
 *
 * @returns {React.JSX.Element} A JSX element that renders the List component.
 */
const ListScreen = () => {
  return <List />;
};

export default ListScreen;
