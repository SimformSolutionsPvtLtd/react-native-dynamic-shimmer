import React from 'react';
import { SampleProfile } from '../../components';

/**
 * SampleProfileScreen Component
 * 
 * This component serves as a wrapper for the `SampleProfile` component, which displays 
 * user profile information. It acts as a dedicated screen in the application's navigation flow.
 * 
 * @returns {React.JSX.Element} A JSX element that renders the SampleProfile component.
 */
const SampleProfileScreen = () => {
  return <SampleProfile />;
};

export default SampleProfileScreen;
