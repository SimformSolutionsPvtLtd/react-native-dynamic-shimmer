import React from 'react';
import { SampleList } from '../../components';

/**
 * SampleListScreen Component
 *
 * This component serves as a wrapper for the `SampleList` component, which displays a list
 * of sample items. It acts as a dedicated screen in the navigation flow of the application.
 *
 * @returns {React.JSX.Element} A JSX element that renders the SampleList component.
 */
const SampleListScreen = () => {
  return <SampleList />;
};

export default SampleListScreen;
