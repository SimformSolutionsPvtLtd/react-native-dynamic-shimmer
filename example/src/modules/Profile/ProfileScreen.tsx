import React from 'react';
import { Profile } from '../../components';

/**
 * ProfileScreen Component
 *
 * This component serves as a wrapper for the `Profile` component, which displays
 * user profile information. It acts as a dedicated screen in the application's navigation flow.
 *
 * @returns {React.JSX.Element} A JSX element that renders the Profile component.
 */
const ProfileScreen = () => {
  return <Profile />;
};

export default ProfileScreen;
