import React from 'react';
import { ScrollViewExample } from '../../components';

/**
 * ScrollViewExampleScreen Component
 * 
 * This component serves as a wrapper for the `ScrollViewExample` component, which displays
 * a scrollable view of content. It acts as a dedicated screen in the application's navigation flow.
 * 
 * @returns {React.JSX.Element} A JSX element that renders the ScrollViewExample component.
 */
const ScrollViewExampleScreen = () => {
  return <ScrollViewExample />;
};

export default ScrollViewExampleScreen;
