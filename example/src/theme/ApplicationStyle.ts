import { StyleSheet } from 'react-native';

/**
 * Application Styles
 *
 * This module contains the styles used throughout the application.
 * The styles are defined using React Native's `StyleSheet` API to ensure
 * consistent styling and performance.
 *
 */
const applicationStyle = StyleSheet.create({
  screen: {
    flex: 1,
  },
});

export default applicationStyle;
