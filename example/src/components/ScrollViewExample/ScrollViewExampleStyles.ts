import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    padding: 20,
  },
  container: {
    alignItems: 'center',
  },
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 20,
    textAlign: 'center',
  },
  paragraph: {
    fontSize: 16,
    lineHeight: 24,
    marginBottom: 20,
    textAlign: 'justify',
  },
  image: {
    width: 300,
    height: 200,
    marginBottom: 20,
    borderRadius: 8,
  },
});

export default styles;
